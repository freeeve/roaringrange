# Runtime deps for the embed Lambda — onnxruntime inference only, NO torch (the
# transformer is exported to ONNX at build time by export_model.py; pooling/dense/
# normalize are replayed in numpy by handler.py). Keeps the image small + the cold
# start fast. Ranges cover the versions the export was validated against.
onnxruntime>=1.20,<2
tokenizers>=0.21,<1
numpy>=1.24,<3
