# Runtime deps for the embed Lambda — onnxruntime inference only, NO torch (the
# model is exported to ONNX at build time by export_model.py). Keeps the image
# small and the cold start fast.
onnxruntime==1.20.1
tokenizers==0.21.0
numpy==2.1.3
