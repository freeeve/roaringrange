# roaringrange

## Task tracking
Tasks live in the central taskman store (`~/.taskman/roaringrange`); follow
the global Task Tracking guidance for the CLI flow. The legacy repo-local
`tasks/` ledger was migrated there in full (numbers 001-078 preserved) and
`/tasks/` is gitignored so it cannot re-track.
